<%@subjects.select("DISTINCT(subject)").each do |s| %>
  <%p=Topic.find_by(:sub=>s.subject) %>
  <%=image_tag p.cover %><br>
 <%=link_to s.subject ,topic_wise_list_path(s.subject),method: :get %><br>
<% end %>
